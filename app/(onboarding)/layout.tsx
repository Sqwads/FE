

import React from 'react';

export default function OnBoardingLayout ({ children }: Readonly<{
    children: React.ReactNode;
  }>)  {
    return (
        <div>
           {children} 
        </div>
    );
}

